# Linux学習記録 Day60
## restore.sh のBats自動テスト追加

### 実践内容
- restore.sh 用の Bats テストファイル（tests/restore.bats）を新規作成
- setup() を追加し、ROOT パスを共通化
- restore.sh の存在確認テストを追加
- restore.sh の実行権限確認テストを追加
- restore.sh の Bash 構文チェックを追加
- restore.bats 単体でテストを実行し、全3テスト成功を確認
- bats . を実行し、既存テストを含む全36テスト成功を確認
- restore.sh 用のテストを追加しても既存の自動テストへ影響がないことを確認

### 間違えたこと
- backup.bats を bash -n で構文チェックしようとし、Bats 専用構文（@test）が原因で構文エラーになると勘違いした
- restore.sh は backup.sh と同じように実行テストを追加できると思っていたが、select や read を使う対話式スクリプトのため、そのままでは自動テストできないことが分かった

### 学んだこと
- Bats ファイルは bash -n ではなく bats コマンドで実行・確認する
- 対話式の Bash スクリプトは、そのままでは自動テストに向かない
- スクリプトの仕様に合わせて、存在確認・実行権限・構文チェックなど適切なテストを作成することが重要であることを学んだ
- 新しい Bats テストを追加した後は、bats . で全体テストを実行し、既存機能へ影響がないことを確認することの重要性を学んだ
